<div class="container">
    <h1 class="text-center "><%= @user.username %>'s profile</h1>

    <div class="text-center mt-4">
        <%= link_to "Edit your profile", edit_user_path(@user), class: "btn btn-primary my-2" %>
    </div>
</div>